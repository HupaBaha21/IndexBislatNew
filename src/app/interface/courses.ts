export interface iCours_short {
    courseName: string
    courseNumber: string
    gender: string
}

export interface iCours_ClearNum {
    courseName: string
    courseNumber: string
    courseUnclearNumber: string
    gender: string
}

export interface iCours_forCycles {
    courseName: string
    courseNumber: string
    courseUnclearNumber: string
}